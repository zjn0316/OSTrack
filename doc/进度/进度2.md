# UGTrack 阶段一对比与阶段二入口进度

## 完整步骤

- [x] 1. 跑完 `uwb_mlp.yaml` 正式阶段一训练
- [x] 2. 新增阶段一 checkpoint 加载验证脚本
- [x] 3. 验证 `uwb_mlp` checkpoint 可加载
- [x] 4. 新增 `uwb_conv1d.yaml`
- [x] 5. 实现 `UWBConv1DEncoder`
- [x] 6. 扩展 `build_ugtrack` 支持 `MODEL.UWB.ENCODER = conv1d`
- [x] 7. 跑通 `uwb_conv1d` smoke test
- [x] 8. 跑完 `uwb_conv1d.yaml` 正式阶段一训练
- [x] 9. 验证 `uwb_conv1d` checkpoint 可加载
- [x] 10. 对比 MLP 与 Conv1D 阶段一结果
- [x] 11. 新增 `vitb_256_mae_ep300_uwb_conv1d_token.yaml`
- [x] 12. 实现阶段二 token-only 融合入口
- [x] 13. 实现阶段二加载 `uwb_conv1d` checkpoint
- [x] 14. 冻结 UWB encoder 并新增参数检查
- [x] 15. 跑阶段二 smoke test
- [ ] 16. 启动阶段二正式训练

## 已完成步骤

- [x] 1. `uwb_mlp` 已训练到 epoch 300。
- [x] 2. 已新增 `script/check_ugtrack_stage1_checkpoint.py`。
- [x] 3. `uwb_mlp` ep0300 checkpoint 已验证，`missing keys: []`，`unexpected keys: []`。
- [x] 4. 已新增 `experiments/ugtrack/uwb_conv1d.yaml`。
- [x] 5. 已在 `lib/models/layers/uwb_encoder.py` 新增 `UWBConv1DEncoder`。
- [x] 6. 已在 `lib/models/ugtrack/ugtrack.py` 支持 `conv1d` encoder。
- [x] 7. `uwb_conv1d` 1 epoch smoke 训练已跑通并能保存 checkpoint。
- [x] 8. `uwb_conv1d` 已训练到 epoch 300。
- [x] 9. `uwb_conv1d` ep0300 checkpoint 已验证，`missing keys: []`，`unexpected keys: []`。
- [x] 10. 对比结论：Conv1D 明显优于 MLP，阶段二优先加载 `uwb_conv1d`。
- [x] 11. 已新增 `experiments/ugtrack/vitb_256_mae_ep300_uwb_conv1d_token.yaml`。
- [x] 12. 已实现 `UGTrack` 阶段二：OSTrack 视觉分支 + UWB token 注入 ViT。
- [x] 13. 已验证可从 `output/checkpoints/train/ugtrack/uwb_conv1d/UGTrack_ep0300.pth.tar` 加载阶段一权重。
- [x] 14. 已验证 `FREEZE_ENCODER=True` 时 UWB 参数无可训练项。
- [x] 15. 已跑通阶段二 token-only smoke test，能完成 train/val、写入 checkpoint。

## 当前进度

```text
总步骤数：16
已完成步骤数：15
进度百分比：93.75%
进度条：●──●──●──●──●──●──●──●──●──●──●──●──●──●──●──○ 93.75%
```

## 进度计算规则

总步骤数 = `## 完整步骤` 区域内的任务数量。

已完成步骤数 = `## 完整步骤` 区域内标记为 `[x]` 或 `[X]` 的任务数量。

进度百分比 = `已完成步骤数 / 总步骤数 * 100%`。

每一个步骤都会成为进度条上的一个节点：

- `●` 表示已完成。
- `○` 表示未完成。

## 使用方式

```powershell
python -X utf8 D:\Project\File2Progress\progress.py .\doc\进度\进度2.md
```

## 当前状态

阶段一 UWB-only 的 MLP 与 Conv1D 两条训练线已经跑通，并且正式训练都已经完成到 epoch 300。

阶段二 token-only 融合正式训练已经启动，当前正在训练：

```text
config: vitb_256_mae_ep300_uwb_conv1d_token
stage-1 checkpoint: output/checkpoints/train/ugtrack/uwb_conv1d/UGTrack_ep0300.pth.tar
BATCH_SIZE: 16
NUM_WORKER: 2
```

当前阶段一不训练：

```text
ViT
ViT-CE
UWB token 融合
第 0 层剪枝
通道注意力调制
tracking loss
```

当前阶段二训练内容：

```text
加载阶段一 UWBConv1DEncoder
冻结 UWB encoder / alpha head / pred head / token head
将 UWB token 拼入 ViT token 序列
训练 OSTrack ViT + tracking head
使用 GIoU + L1 + Focal tracking loss
```

## 阶段一训练结果

```text
uwb_mlp:
  checkpoint: output/checkpoints/train/ugtrack/uwb_mlp/UGTrack_ep0300.pth.tar
  log: output/logs/ugtrack-uwb_mlp.log
  epoch 300 val Loss/uwb_total: 0.10301
  epoch 300 val Loss/uwb_pred: 0.00066
  epoch 300 val Loss/uwb_alpha: 0.01405

uwb_conv1d:
  checkpoint: output/checkpoints/train/ugtrack/uwb_conv1d/UGTrack_ep0300.pth.tar
  log: output/logs/ugtrack-uwb_conv1d.log
  epoch 300 val Loss/uwb_total: 0.04994
  epoch 300 val Loss/uwb_pred: 0.00007
  epoch 300 val Loss/uwb_alpha: 0.00932
```

结论：

```text
Conv1D 明显优于 MLP。
阶段二优先加载 uwb_conv1d 阶段一 checkpoint。
```

阶段二优先使用：

```text
--script_prv ugtrack --config_prv uwb_conv1d
```

## 已验证命令

```bash
conda activate ostrack
python tracking/train.py --script ugtrack --config uwb_mlp --save_dir ./output --mode single --use_wandb 0
python script/check_ugtrack_stage1_checkpoint.py --config uwb_mlp --checkpoint ./output/checkpoints/train/ugtrack/uwb_mlp/UGTrack_ep0300.pth.tar
python tracking/train.py --script ugtrack --config uwb_conv1d --save_dir ./output --mode single --use_wandb 0
python script/check_ugtrack_stage1_checkpoint.py --config uwb_conv1d --checkpoint ./output/checkpoints/train/ugtrack/uwb_conv1d/UGTrack_ep0300.pth.tar
```

## 阶段二入口规划

阶段二最小实验已经完成 smoke test：

```text
vitb_256_mae_ep300_uwb_conv1d_token.yaml
```

含义：

```text
加载阶段一 UWB encoder
冻结 UWB encoder
不做第 0 层剪枝
不做通道注意力调制
只把 UWB token 加入 ViT
```

命令形式：

```bash
conda activate ostrack
python tracking/train.py --script ugtrack --config vitb_256_mae_ep300_uwb_conv1d_token --script_prv ugtrack --config_prv uwb_conv1d --save_dir ./output --mode single --use_wandb 0
```

已验证 smoke 命令：

```bash
conda activate ostrack
python script/check_ugtrack_stage2_token.py --config vitb_256_mae_ep300_uwb_conv1d_token --stage1_config uwb_conv1d --batch_size 1
python script/check_ugtrack_stage1_train_smoke.py --config vitb_256_mae_ep300_uwb_conv1d_token --save_dir ./output_smoke_stage2_token2 --samples 2 --batch_size 1 --script_prv ugtrack --config_prv uwb_conv1d --prv_save_dir ./output
```

已验证输出：

```text
stage1 checkpoint epoch: 300
trainable uwb params: []
pred_boxes: [B, 1, 4]
score_map: [B, 1, 16, 16]
uwb_token: [B, 1, 768]
smoke train/val 正常结束
smoke checkpoint 正常写入
```

当前只剩下一步：

```text
等待阶段二正式训练完整结束并验证 checkpoint
```

## 阶段二正式训练记录

启动命令：

```bash
conda activate ostrack
python tracking/train.py --script ugtrack --config vitb_256_mae_ep300_uwb_conv1d_token --script_prv ugtrack --config_prv uwb_conv1d --save_dir ./output --mode single --use_wandb 0
```

当前训练中观察：

```text
epoch 1, iter 50 / 312:
  FPS: 25.1
  DataTime: 0.097
  ForwardTime: 0.501
  TotalTime: 0.638
  Loss/total: 11.36875
  Loss/giou: 1.07208
  Loss/l1: 0.22688
  Loss/location: 8.09020
  IoU: 0.12677
  UWB/alpha_mean: 0.92774

epoch 1, iter 100 / 312:
  FPS: 27.3
  DataTime: 0.050
  ForwardTime: 0.495
  TotalTime: 0.586
  Loss/total: 7.42560
  Loss/giou: 0.87460
  Loss/l1: 0.15826
  Loss/location: 4.88510
  IoU: 0.27173
  UWB/alpha_mean: 0.92957

epoch 1, iter 150 / 312:
  FPS: 28.0
  DataTime: 0.034
  ForwardTime: 0.496
  TotalTime: 0.571
  Loss/total: 5.72092
  Loss/giou: 0.74792
  Loss/l1: 0.12341
  Loss/location: 3.60804
  IoU: 0.36801
  UWB/alpha_mean: 0.93074

epoch 1, iter 200 / 312:
  FPS: 28.3
  DataTime: 0.026
  ForwardTime: 0.498
  TotalTime: 0.565
  Loss/total: 4.75389
  Loss/giou: 0.66794
  Loss/l1: 0.10364
  Loss/location: 2.89979
  IoU: 0.42989
  UWB/alpha_mean: 0.93074

epoch 1, iter 250 / 312:
  FPS: 28.5
  DataTime: 0.021
  ForwardTime: 0.500
  TotalTime: 0.562
  Loss/total: 4.13757
  Loss/giou: 0.61344
  Loss/l1: 0.09099
  Loss/location: 2.45572
  IoU: 0.47265
  UWB/alpha_mean: 0.93038
```

当前判断：

```text
训练速度正常，DataTime 已降到较低水平。
Loss/total 持续下降。
IoU 持续上升。
UWB/alpha_mean 稳定在 0.93 左右。
当前配置 BATCH_SIZE=16、NUM_WORKER=2 可以继续使用。
```

## 已知环境注意事项

```text
运行前先执行 conda activate ostrack。
Windows 环境中 NUM_WORKER=0 更稳定。
程序退出时可能出现 wandb 临时目录清理 WinError 5。
如果训练主体已完成，并且 checkpoint/log 已经写入，可以先忽略该退出清理报错。
```

## 进度维护约定

每完成一个阶段性任务后，需要同步更新本文件，至少记录：

```text
完成内容
运行命令
生成产物
关键 loss / shape / checkpoint 验证结果
下一步优先级
```
