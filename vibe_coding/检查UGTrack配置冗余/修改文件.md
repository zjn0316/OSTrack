# 修改文件

- `lib/config/ugtrack/config.py`
  - 删除未被 UGTrack 代码读取的 `cfg.TRAIN.SCHEDULER.DECAY_RATE`。

- `doc/UGTrack/YAML说明.md`
  - 新增冗余配置检查说明，解释调度器当前只读取 `TYPE` 和 `LR_DROP_EPOCH`。

- `vibe_coding/检查UGTrack配置冗余/`
  - 新增本次任务记录。
