# 进度

- 已阅读 `AGENTS.md` 和 `doc/UGTrack设计文档.md`。
- 已将 Stage-1 UWB encoder 改为 MLP/GRU/TCN 三选一，默认 TCN 输出 128 维。
- 已将 UWB head 改为 `pred_uv / pred_delta_uv / uwb_conf_logit / uwb_conf_pred / uwb_token` 风格。
- 已将 Stage-1 损失入口改为 L1 + BCEWithLogitsLoss。
- 已重建 `lib/config/ugtrack/config.py` 与两个 Stage-1 TCN 实验配置。
