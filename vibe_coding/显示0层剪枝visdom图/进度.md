# 进度

- 已读取 `AGENTS.md` 与 `UGTrack.md`。
- 已移除 UGTrack debug 中 `search_before_pruning` 的 Visdom 窗口注册。
- 已移除本地 debug 中 `search_before_pruning` 目录创建与图片保存。
- 已保留第 0 层剪枝后的 Visdom 窗口：`uwb_pruned_search_layer0`。
- 已将 `stage2_tcn_residual_seq10_ep1.yaml` 与 `stage2_tcn_residual_seq10_ep10_ce.yaml` 的 `UWB_PRUNE_ENABLE` 改为 `True`。

