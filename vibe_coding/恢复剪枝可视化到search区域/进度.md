# 进度

## 2026-05-01

- 已读取 `AGENTS.md` 与 `doc/UGTrack设计文档.md`。
- 合并了 `vibe_coding/修复0层剪枝可视化padding问题` 的记录；该目录对应一次误判修复，最终已回滚。
- 误判内容：曾将 debug 可视化改为按 `x_amask_arr` 过滤 padding token，只显示原图有效区域内被剪掉的 token。
- 已撤销该误判逻辑：第 0 层剪枝可视化重新直接作用在 256×256 search crop 的 16×16 token 网格上。
- 保留正确逻辑：UWB 序列仍映射到当前 search crop 坐标系，因为剪枝器使用的 `pred_uv` 应与 search token 网格同坐标系。
- 已更新 `doc/UGTrack设计文档.md` 的 debug 可视化说明。
- 最终状态：剪枝发生在 search 区域上，padding 也属于 search crop token 网格的一部分，不再按原图有效区域过滤。
