# 问题及解决措施

| 问题 | 解决措施 |
|------|---------|
| Stage-1 使用独立的 `UWBStage1Dataset`，只返回 UWB 数据无图像裁剪/增强，与 Stage-2 数据处理方式不一致 | 将 Stage-1 数据管线统一为 `UWBTrackingSampler` + `UWBProcessing`，两阶段共享相同的图像裁剪/增强流程。Stage-1 Actor 前向仍只取 `search_uwb_seq`，新增图像字段不参与计算 |
| `UWBStage1Dataset` 被删除后，import 残留导致引用错误 | 移除 `base_functions_ugtrack.py` 中的 `from lib.train.data.uwb_stage1_dataset import UWBStage1Dataset` |
| 设计文档与代码实际状态严重脱节（多处标注 ⏳/🟡 但代码已实现） | 全面审查代码状态，逐节更新设计文档，对齐 6 项完成状态 |
