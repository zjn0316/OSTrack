# 进度

- 已读取 `AGENTS.md` 和 `UGTrack.md`。
- 已参考现有 Stage-1 配置 `experiments/ugtrack/uwb_tcn_residual_seq10_ep100_l1_bce05.yaml`。
- 已在 `experiments/ugtrack` 生成 11 个 Stage-1 消融实验 YAML。
- 准备按 YAML 顺序串行运行 Stage-1 训练实验。
- 已新增 `script/run_stage1_uwb_ablation.ps1`，用于串行执行 Stage-1 训练、测试和分析。
