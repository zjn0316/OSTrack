# 进度

## 2026-05-01

- 已读取 `AGENTS.md` 与 `doc/UGTrack设计文档.md`。
- 已查看 `debug/masked_search.jpg` 与 `debug/masked_search (1).jpg`。
- 已用同一张 `BlurBody` 图、同一 bbox、同一 `search_factor/search_size` 直接调用 `sample_target()`，确认 OSTrack 写法和 UGTrack 写法输出逐像素一致。
- 已定位差异来源：UGTrack 测试时没有加载 OSTrack 视觉 tracker checkpoint，导致第一帧后 `self.state` 漂移，后续 search crop 变成大面积 padding。
- 已修改 `build_ugtrack(training=False)`，测试阶段也会加载配置中的 OSTrack 视觉 checkpoint。
- 已为 UGTrack debug yaml 增加 OSTrack ViT-B base checkpoint 路径。
