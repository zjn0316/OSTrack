# UGTrack 测试侧流程进度

## 完整步骤

- [x] 1. 梳理 OSTrack 原版 test 入口、参数文件和 tracker 调用链路
- [x] 2. 确认 UGTrack test 侧已有文件状态
- [x] 3. 新增或修正 UGTrack test 参数文件，支持阶段二 token-only 配置
- [x] 4. 新增或修正 UGTrack tracker，支持加载阶段二 checkpoint
- [x] 5. 在 test 侧读取并构建 search UWB 历史序列
- [x] 6. 在 test 侧复用阶段二模型 forward，传入 template、search、search_uwb_seq
- [x] 7. 验证单帧 forward 输出 pred_boxes / score_map / uwb_token shape 正确
- [ ] 8. 跑通单序列 debug 测试
- [ ] 9. 跑通 OTB100_UWB 小规模测试
- [ ] 10. 确认测试结果文件正常保存
- [ ] 11. 跑官方或本项目评估脚本，得到成功率 / 精度等指标
- [ ] 12. 对比 baseline OSTrack、UGTrack MLP、UGTrack Conv1D token-only 的测试指标
- [ ] 13. 记录测试命令、checkpoint、结果路径和核心指标
- [ ] 14. 根据测试结果决定是否进入下一阶段：剪枝 / 通道注意力调制 / Conv1D token 改进

## 已完成步骤

- [x] 1. 已梳理 `tracking/test.py -> lib.test.evaluation.Tracker -> lib.test.parameter -> lib.test.tracker` 测试调用链。
- [x] 2. 已确认 `lib/test/parameter/ugtrack.py` 和 `lib/test/tracker/ugtrack.py` 原先为空，需要补齐。
- [x] 3. 已新增 `lib/test/parameter/ugtrack.py`，可读取 `experiments/ugtrack/*.yaml` 并定位 `UGTrack_epXXXX.pth.tar`。
- [x] 4. 已新增 `lib/test/tracker/ugtrack.py`，可构建阶段二 UGTrack 并加载 checkpoint。
- [x] 5. 已扩展 `lib/test/evaluation/otb100uwbdataset.py`，在初始化信息中传递 `uwb_noise.txt`、`uwb_gt.txt`、`alpha_gt.txt` 路径。
- [x] 6. 已在 UGTrack tracker 中按当前帧号构建 `search_uwb_seq [1, T, 2]` 并传入 `network.forward(stage=2)`。
- [x] 7. 已新增并运行 `script/check_ugtrack_test_tracker.py`，验证单帧输出 shape 正确。

## 当前进度

```text
总步骤数：14
已完成步骤数：7
进度百分比：50.00%
进度条：●──●──●──●──●──●──●──○──○──○──○──○──○──○ 50.00%
```

## 进度计算规则

总步骤数 = `## 完整步骤` 区域内的任务数量。

已完成步骤数 = `## 完整步骤` 区域内标记为 `[x]` 或 `[X]` 的任务数量。

进度百分比 = `已完成步骤数 / 总步骤数 * 100%`。

每一个步骤都会成为进度条上的一个节点：

- `●` 表示已完成。
- `○` 表示未完成。

## 使用方式

```powershell
python -X utf8 D:\Project\File2Progress\progress.py .\doc\进度\进度3.md
```

## 当前目标

训练侧已经打通：

```text
阶段一：UWB-only MLP / Conv1D
阶段二：vitb_256_mae_ep300_uwb_conv1d_token
```

接下来需要打通 test 侧闭环，确认训练完成后的 checkpoint 能真实用于跟踪测试。

## 测试侧预期输入

```text
阶段二 checkpoint:
output/checkpoints/train/ugtrack/vitb_256_mae_ep300_uwb_conv1d_token/UGTrack_epXXXX.pth.tar

阶段一 UWB checkpoint:
output/checkpoints/train/ugtrack/uwb_conv1d/UGTrack_ep0300.pth.tar

测试配置:
experiments/ugtrack/vitb_256_mae_ep300_uwb_conv1d_token.yaml

测试数据:
OTB100_UWB
```

## 测试侧预期输出

```text
跟踪结果文件
测试日志
可复现的测试命令
成功率 / 精度 / AUC 等评估指标
与 baseline 的对比表
```

## 初步计划

优先走最小闭环：

```text
1. 只测单序列。
2. 确认 UWB 序列读取和归一化正确。
3. 确认 tracker 每帧都能传入 search_uwb_seq。
4. 确认模型能输出 bbox。
5. 再扩展到完整 OTB100_UWB。
```

## 需要重点检查

```text
test 侧是否加载阶段二 checkpoint，而不是只加载阶段一 checkpoint
test 侧 UWB 坐标归一化是否和 train 侧一致
search_uwb_seq shape 是否为 [1, T, 2]
UWB token 是否实际进入 ViT forward
tracker 输出 bbox 是否映射回原图坐标
结果文件格式是否符合评估脚本要求
```

## 当前阻塞

```text
阶段二正式训练仍在进行中。
完整测试需要等待阶段二 checkpoint 生成。
```

## 已验证命令

当前使用阶段二 smoke checkpoint 验证 test 侧最小单帧链路：

```bash
conda activate ostrack
python script/check_ugtrack_test_tracker.py --config vitb_256_mae_ep300_uwb_conv1d_token --checkpoint ./output_smoke_stage2_loader_fix/checkpoints/train/ugtrack/vitb_256_mae_ep300_uwb_conv1d_token_smoke/UGTrack_ep0001.pth.tar --dataset otb100_uwb --sequence 0
```

验证结果：

```text
dataset: otb100_uwb
sequence: Liquor
has init_uwb_noise_path: True
uwb seq shape: (1, 5, 2)
output pred_boxes: (1, 1, 4)
output score_map: (1, 1, 16, 16)
output uwb_token: (1, 1, 768)
target_bbox: 正常输出
```

## 新增/修改文件

```text
lib/test/parameter/ugtrack.py
lib/test/tracker/ugtrack.py
lib/test/evaluation/otb100uwbdataset.py
script/check_ugtrack_test_tracker.py
```

## 进度维护约定

每完成一个测试侧阶段性任务后，需要同步更新本文件，至少记录：

```text
完成内容
运行命令
输入 checkpoint
测试数据范围
输出结果路径
关键 shape / 指标 / 报错信息
下一步优先级
```
