# 问题及解决措施

## 问题

- 非 CE 模式命名中带 `noce` 冗余，不符合当前实验命名期望。
- 当前没有 `stage2_tcn_residual_seq10_ep1.yaml`，也没有对应的 `UGTrack_ep0001.pth.tar`，不能直接运行 ep1 test。

## 解决措施

- 将非 CE YAML 与 checkpoint 目录统一改为 `stage2_tcn_residual_seq10_ep10`。
- 新增 ep1 YAML，后续先跑 1 epoch 训练生成 checkpoint，再运行 debug test。

