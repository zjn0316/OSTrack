# 进度

## 2026-05-01

- 已读取 `AGENTS.md` 与 `doc/UGTrack设计文档.md`。
- 已确认此前只完成 `uwb_token` 嵌入，没有单独的 UWB token 位置编码。
- 已在 `lib/models/ugtrack/vit_uwb.py` 新增可学习 `uwb_pos_embed`。
- 已在 `lib/models/ugtrack/vit_ce_uwb.py` 新增可学习 `uwb_pos_embed`。
- 已在非 CE 与 CE 路径中将 `uwb_token + uwb_pos_embed` 后再注入 transformer token 序列。
- 已将 `uwb_pos_embed` 加入 `no_weight_decay()`。
- 已更新 `doc/UGTrack设计文档.md`。
