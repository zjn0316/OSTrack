# 问题及解决措施

## 2026-05-01

- 问题：需要确认“UWB token + pos embedding”是否已经在代码中真实完成，而不只依赖设计文档状态。
- 措施：通过代码搜索和关键文件读取确认：
  - `UWBBranch.forward()` 输出 `uwb_token`。
  - `UGTrack.forward()` 将 `uwb_token` 透传到 tracker。
  - 非 CE backbone `VisionTransformerUWB` 定义并使用 `uwb_pos_embed`。
  - CE backbone `VisionTransformerCEUWB` 定义并使用 `uwb_pos_embed`，并通过 template-side 索引保护 UWB token。
