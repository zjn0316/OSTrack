# 进度

## 2026-05-01

- 已读取 `AGENTS.md` 与 `doc/UGTrack设计文档.md`。
- 已定位 `ostrack` 官方流程被 UWB 参数污染的问题：`lib/models/ostrack/ostrack.py` 的 `OSTrack.forward()` 向官方 backbone 传入了 `uwb_token`。
- 已移除 `lib/models/ostrack/ostrack.py` 中的 `uwb_token` 参数和 backbone 调用参数。
- UGTrack 的 UWB 参数透传仍保留在 `lib/models/ugtrack/ostrack_uwb.py`。
- 已确认官方 OSTrack 路径中不再出现 `uwb_token / pred_uv / uwb_conf_pred / UWB` 关键字。
- 已完成轻量前向冒烟测试，`OSTrack.forward()` 可正常输出 `pred_boxes / score_map / backbone_feat`。
- 已清理冒烟测试产生的 `__pycache__`。
