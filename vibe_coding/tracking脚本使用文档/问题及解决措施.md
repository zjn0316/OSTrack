# 问题及解决措施

## 问题 1：`analysis_results.py` 无命令行参数

该脚本当前通过源码中的 `trackerlist` 固定比较对象。

解决措施：在文档中明确说明该脚本需要通过修改 `trackers.extend(...)` 列表来调整比较对象。

## 问题 2：checkpoint 示例路径可能与实际训练轮次不一致

不同训练配置和 epoch 会生成不同 checkpoint 文件名。

解决措施：在文档中提示先检查 `output/checkpoints/train/ugtrack/<config_name>/` 下的实际文件。

