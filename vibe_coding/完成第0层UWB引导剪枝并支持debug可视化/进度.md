# 进度

## 2026-05-01

- 已读取 `AGENTS.md` 与 `doc/UGTrack设计文档.md`。
- 已新增第 0 层 UWB 引导剪枝模块 `UWBGuidedPruner`。
- 已按 AGENTS.md 第 8 条将剪枝模块迁移到 `lib/models/ugtrack/uwb_pruning.py`。
- 已新增 `lib/models/ugtrack/vit_uwb.py` 和 `lib/models/ugtrack/ostrack_uwb.py`，承载 UGTrack 专用兼容逻辑。
- 已还原 `lib/models/ostrack/base_backbone.py` 与 `lib/models/ostrack/ostrack.py`，避免污染原始 OSTrack 实现。
- 已将 `pred_uv` 与 `uwb_conf_pred` 从 UGTrack 传入视觉 backbone。
- 已在非 CE backbone 与 CE UWB backbone 中接入剪枝、恢复和 debug 输出。
- 已在 UGTrack 测试 tracker 中增加 `uwb_pruned_search_layer0` visdom 图像。
- 已新增更直观的 visdom 窗口名：`search_before_pruning` 与 `search_after_layer0_pruning`，用于对比剪枝前后 search 图。
- 已支持 visdom 不可用时在本地 `debug/search_before_pruning/` 和 `debug/search_after_layer0_pruning/` 保存剪枝前后 search 图。
- 已更新 `doc/UGTrack设计文档.md`，补充 `--debug 1` 在 visdom 与本地降级模式下的剪枝可视化输出说明。
- 已新增 token-prune 调试配置 `experiments/ugtrack/uwb_tcn_residual_seq10_ep10_prune_test.yaml`。
- 已将现有 `experiments/ugtrack/uwb_tcn_residual_seq10_ep10_test.yaml` 调整为可直接测试第 0 层剪枝的 Stage-2 debug 配置。
- 已更新 `doc/UGTrack/YAML说明.md` 与 `doc/UGTrack设计文档.md`。
