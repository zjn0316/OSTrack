# 问题及解决措施

## 2026-05-01

| 问题 | 解决措施 |
| --- | --- |
| `修复0层剪枝可视化padding问题` 目录记录的是一次误判修复，容易和最终实现冲突 | 已合并到本目录，并明确标注该修复已回滚 |
| 曾错误地按 `sample_target()` 返回的 `x_amask_arr` 过滤 padding token，使可视化语义变成“原图有效区域剪枝” | 已移除过滤逻辑，剪枝索引原样映射到 search crop token 网格 |
| 上一轮可视化过滤 padding token，语义变成只看原图有效区域 | 移除 `_filter_removed_indexes_to_valid_tokens()`，剪枝索引原样映射到 search crop token 网格 |
| 第 0 层剪枝应在搜索区域坐标系解释 | 保留 UWB 到 search crop 的坐标映射，并让可视化也回到 search crop |
| 遵守不污染官方 OSTrack | 本次只修改 UGTrack 测试 tracker 和 UGTrack 设计文档 |
