# 问题及解决措施

## 问题

- 日志中同一验证 epoch 出现重复记录，例如 `val: 90` 出现两次。
- 训练损失最低出现在 epoch 100，但验证损失不是最低。

## 解决措施

- 对相同 split 与 epoch 的重复记录进行去重，保留相同指标中的一条用于比较。
- 以验证集 `Loss/uwb_total` 作为最佳 epoch 的主要判据，训练损失仅作为辅助参考。

